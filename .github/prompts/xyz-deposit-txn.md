You are executing a banking deposit workflow using the `new_api_mcp` MCP server.

## Steps
1. Call `run_xyz_bank_deposit` with default parameters (deposit_amount: 200).
   The server runs Playwright internally against the XYZ Bank demo site and returns the result.

## Output
Summarize: starting_balance, deposited_amount, ending_balance from the tool response.
